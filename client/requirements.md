## Packages
recharts | For visualizing NCI and Whale Flow metrics over time
framer-motion | For smooth terminal entry animations and transitions
date-fns | For formatting timestamps in the dashboard

## Notes
The backend provides /api/metrics and /api/brief endpoints.
The UI should feel like a "Operator" terminal: dark, neon green/cyan accents, monospaced fonts where appropriate.
Data fetching will use the hooks generated based on the shared schema.
